# =============================================================================
# Simplified Catan Game Environment Configuration
# =============================================================================

# DEVELOPMENT (Local)
# These are automatically configured in docker-compose.yaml for development

# STAGING ENVIRONMENT (Private Network - test.ts.chis.dev)
JWT_SECRET_STAGING=your-staging-jwt-secret-here

# PRODUCTION ENVIRONMENT (Public Network - catan.chis.dev)  
JWT_SECRET_PROD=your-production-jwt-secret-here
MONGODB_URI_PROD=mongodb://mongodb:27017/catan-game-prod

# =============================================================================
# Instructions:
# 
# 1. For Development: Just run "Dev: Start Full Stack" task - no setup needed
# 2. For Staging: Set JWT_SECRET_STAGING, then run "Staging: Deploy" task
# 3. For Production: Set JWT_SECRET_PROD and MONGODB_URI_PROD, then run "Prod: Deploy" task
# 
# The Docker Compose setup handles all the networking and container configuration.
# =============================================================================
